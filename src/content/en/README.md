# content/en

English copy, translated from `../es/`.

Filenames mirror the Spanish folder exactly. When a Spanish file has no
English counterpart, that translation is missing — the gap should be obvious
from the file listing rather than hidden behind a runtime fallback.
