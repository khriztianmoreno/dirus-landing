# lib/utils

Small shared helpers that have no better home.

This folder earns its keep only if it stays small. A `utils/` that grows past
a handful of functions is usually hiding a missing module: when several
helpers turn out to be about the same subject, move them into a named folder
instead of leaving them here.
