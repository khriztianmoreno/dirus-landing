# content

Marketing copy, kept out of the components that render it.

One folder per locale — `es/` and `en/` — with matching filenames so a
missing translation is visible as a missing file rather than a silent
fallback to the wrong language.

Spanish is the source of truth: DIRUS sells to brokers in Latin America, and
the English copy is a translation of it, not a parallel original.
