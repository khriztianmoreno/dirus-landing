# components/ui

Presentational primitives: button, card, badge, input, icon.

Nothing here may reference DIRUS, insurance or any specific page. A
primitive that could not be dropped into an unrelated product without edits
belongs in `sections/` instead.

These are the components worth testing thoroughly — they are used everywhere,
so a defect here multiplies.
